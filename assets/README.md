# Assets

## cat.png

把对比工具右下角那只猫猫的图片放在这里,文件名必须是 `cat.png`。

- 建议尺寸: 宽度 300-600 px,背景透明
- 放好之后 GitHub Pages 会自动部署,不需要改任何代码
- 图片缺失时会自动隐藏,只保留对话气泡,不会报错

如果要换成其它文件名或格式,改 `index.html` 里 `.cat-mascot img` 的 `src` 即可。
